---
- name: setup web servers
  hosts: webservers
  remote_user: zoomhub
  vars_files:
    - [ 'vars.yml', 'vars.default.yml' ]
    - secrets.vault.yml
  sudo: yes
  roles:
  #  - storage-volume // commenting this out as it's not functional
    - nodesource.node
    - web
    - DavidWittman.redis
    - vips

