stanzas:
  - type: webapp
    exec: ../zoomhub
    hosts:
      - openzoom.net
      - openzoom.org
      - staging.zoom.it
      - staging.zoomhub.net
      - staging.zoomhub.org
      - teamteam.team
      - v2.zoom.it
      - v2.zoomhub.net
      - v2.zoomhub.org
    env:
      DATA_PATH: '/mnt/datavolume/zoomhub/data'
      HASHIDS_SALT: 'CHANGE-ME-IN-PRODUCTION'
      BASE_URI: 'http://v2.zoomhub.net'
