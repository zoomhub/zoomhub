FROM ubuntu:18.04

RUN mkdir -p /opt/app
WORKDIR /opt/app

RUN apt-get upgrade -y && \
    apt-get update -y && \
    apt-get install -y \
      ca-certificates \
      libgmp-dev \
      libpq-dev

ADD public /opt/app/public
ADD zoomhub /opt/app

EXPOSE 5000

CMD ["/opt/app/zoomhub"]
